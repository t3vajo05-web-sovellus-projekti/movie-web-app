name: Deploy app to cloud server

on:
  push:
    branches: 
      - main
      - 124-cicd-workflown-määrittely
  pull_request:
    types: [closed]
    branches:
      - main
      - 124-cicd-workflown-määrittely

jobs:
  deploy:
    if: github.event_name == 'push' || github.event.pull_request.merged == true
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run deploy script
        run: bash ~/deploy.sh
